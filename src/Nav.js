import React from 'react';
import Button from './Button';

const Nav = () => {
  return (
    <div>
      <Button title="work" />
      <Button title="about"/>
      <Button title="contact"/>
    </div>
  )
}

export default Nav;
